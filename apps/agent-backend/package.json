{
  "name": "@dignity/agent-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Dignity agent backend — Fastify 5 MCP tool service",
  "scripts": {
    "dev": "tsx --watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dignity/db": "workspace:*",
    "@dignity/shared-types": "workspace:*",
    "fastify": "^5.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "tsx": "^4.17.0",
    "eslint": "^8.57.0"
  }
}
